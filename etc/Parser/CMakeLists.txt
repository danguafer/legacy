CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT("VeritasetcParser")

INCLUDE("../../Silexars.cmake")

AUX_SOURCE_DIRECTORY(. SOURCES)
AUX_SOURCE_DIRECTORY(libbnf SOURCES)

ADD_SILEXARS_LIBRARY(${PROJECT_NAME} ${SOURCES})
