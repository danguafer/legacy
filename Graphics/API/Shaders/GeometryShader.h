#pragma once

#include "Shader.h"

namespace Silexars {
    namespace Graphics {
        namespace API {
            namespace Shaders {
                class GeometryShader : public Shader {
                    public:
                        GeometryShader(const char *source);
                };
            }
        }
    }
}
