#pragma once

#include "Shader.h"

namespace Silexars {
    namespace Graphics {
        namespace API {
            namespace Shaders {
                class FragmentShader : public Shader {
                    public:
                        FragmentShader(const char *source);
                };
            }
        }
    }
}
